
var config = {
    width: 256,
    height: 272,
    bacgroundColor: 0x000000,
    scene: [Scene1, Scene2],
    pixelArt: true
}

var gameSettings = {
    playerSpeed: 3
}

var game = new Phaser.Game(config);
